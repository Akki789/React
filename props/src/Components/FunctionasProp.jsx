import React from 'react'

export default function FunctionasProp({onPress}) {
  return (
    <div>
        <h2>📝 Assignment 10 — Pass a function as a prop</h2>
        <button onClick={onPress}>Press</button>
    </div>
  )
}
